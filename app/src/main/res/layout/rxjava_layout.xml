<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical" android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:weightSum="4"
    android:gravity="center_horizontal"
    android:background="?attr/bg_color">

    <TextView
        android:id="@+id/rxjava_event_tv"
        android:background="@drawable/trade_bd_edit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_weight="3"
        android:layout_margin="10dp"
        android:tag="@string/skin_font_color"
        android:textColor="?attr/font_color"
        android:textSize="@dimen/font_tv_big"/>

    <TextView
        android:id="@+id/rxjava_event_submit"
        android:layout_width="wrap_content"
        android:paddingLeft="10dp"
        android:paddingRight="10dp"
        android:layout_height="40dp"
        android:layout_margin="10dp"
        android:text="发送请求"
        android:gravity="center"
        android:tag="@string/skin_button_color"
        android:textColor="?attr/button_font_color"
        android:background="?attr/button_bg_color"

        />
</LinearLayout>